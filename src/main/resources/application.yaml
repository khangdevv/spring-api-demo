spring:
  profiles:
    active: dev

  main:
    banner-mode: off

  datasource:
    url: jdbc:mysql://localhost:3306/yoedu_demo?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true&zeroDateTimeBehavior=CONVERT_TO_NULL
    username: root
    password:
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

  security:
    user:
      name: admin

server:
  port: 8080
