$kind: http-request
url: "{{baseUrl}}/api/schedule-slots/{{scheduleSlotId}}"
method: PUT
headers:
  Content-Type: application/json
  Authorization: Bearer {{token}}
body:
  type: json
  content: |-
    {
      "slotCode": "CAL999",
      "weekday": 2,
      "startTime": "18:15:00",
      "endTime": "19:45:00",
      "note": "Ca toi thu 2 da cap nhat"
    }
order: 4000
